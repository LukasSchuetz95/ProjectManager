@model ProjectManager.Web.Models.ViewModel.TasksListViewModel
@{
    ViewData["Title"] = "InProgressList";
}

<h3>Tasks in Bearbeitung</h3>

<form asp-controller="Tasks" asp-action="List">
    <div class="form-group">
        <input asp-for="FilterTaskName" class="form-control" />
        <span asp-validation-for="FilterTaskName" class="text-danger"></span>
    </div>
    <input type="submit" value="Filter Task Name" class="btn btn-primary" />
</form>


<table class="table">
    <thead>
        <tr>
            <th>Task</th>
            <th>Deadline</th>
            <th>Taskname</th>
        </tr>
    </thead>
    <tbody>
        @if (Model.FilterTaskName != null)
        {
            @foreach (var task in Model.ProcessingTasks)
            {
                <tr>
                    <td>@task.TaskName</td>
                    <td>@task.Deadline</td>
                    <td>@task.TaskName</td>
                    <td>
                        <a asp-controller="Tasks" asp-action="Details" asp-route-taskId="@task.Id">Details</a> |
                    </td>
                </tr>

            }
        }
        else
        {
            @foreach (var task in Model.ProcessingTasks)
            {
                <tr>
                    <td>@task.TaskName</td>
                    <td>@task.Deadline</td>
                    <td>@task.TaskName</td>
                    <td>
                        <a asp-controller="Tasks" asp-action="Details" asp-route-taskId="@task.Id">Details</a> |
                    </td>
                </tr>

            }
        }
    </tbody>
</table>


@*<table class="table">
        <thead>
            <tr>
                <th>Taskname</th>
                <th>Projekt</th>
                <th>Priorität</th>
                <th>Von</th>
                <th>Bis</th>
                <th>Deadline</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var task in Model.ProcessingTasks)
            {
                <tr>
                    <td>@task.TaskName</td>
                    <td>@task.Project.ProjectName</td>
                    <td>@task.Priority</td>
                    <td>@task.Startdate</td>
                    <td>@task.Enddate</td>
                    <td>@task.Deadline</td>
                    <td>
                        <a asp-controller="Tasks" asp-action="Details" asp-route-taskId="@task.Id">Details</a>
                        <a asp-controller="Tasks" asp-action="Edit" asp-route-taskId="@task.Id">Edit</a>
                        <a asp-controller="Tasks" asp-action="Delete" asp-route-taskId="@task.Id">Task Löschen</a> |
                    </td>
                </tr>
            }
        </tbody>
        <a asp-area="" asp-controller="Tasks" asp-action="List" class="btn btn-primary">Zurück</a>
    </table>*@
