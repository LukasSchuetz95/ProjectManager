@model ProjectManager.Web.Models.ViewModel.Employees.EmployeesDeleteOrPassViewModel

<h3>Are you sure you want to delete this?</h3>
<div>
    <h4>EmployeesDeleteOrPassViewModel</h4>
    <hr />
    <dl class="dl-horizontal"></dl>

    <div class="form-group">
        <label class="control-label">Mitarbeiter</label>
        <select asp-for="RecipientEmployee.Id" asp-items="@Model.EmployeeSelectList" class="form-control"></select>
        <span asp-validation-for="RecipientEmployee.Id" class="text-danger"></span>
    </div>

    <form asp-action="DeleteOrPass">
        <input type="submit" value="Delete" class="btn btn-default" /> |
        <a asp-action="Index">Back to List</a>
    </form>
</div>
